name: favicon
description: Library for finding favicons in a website, either in a <link> or by predefined URL.
version: 1.1.2
homepage: https://github.com/marcjoha/favicon

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  http:
  html:
  image:

dev_dependencies:
  test: ^1.22.2
